import { View, Text, StyleSheet } from "react-native";

const CategoriesChart = () => {
  return (
    <View>
      <Text style={styles.title}>Categories chart</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  title: {
    color: "white",
  },
});

export default CategoriesChart;
